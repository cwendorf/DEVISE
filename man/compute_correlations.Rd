% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computer.R
\name{compute_correlations}
\alias{compute_correlations}
\title{Compute Correlation or Covariance Matrices}
\usage{
compute_correlations(data, ..., type = "cor", method = "pearson")
}
\arguments{
\item{data}{A data frame containing the variables of interest.}

\item{...}{One or more expressions:
\itemize{
\item A formula (e.g., \code{~ group} or \code{y1 + y2 ~ group}) to specify grouping and optionally variables.
\item One or more bare variable names (e.g., \verb{x1, x2}).
\item If omitted, all numeric variables in \code{data} are used.
}}

\item{type}{Character string indicating whether to compute \code{"cor"} (correlation) or \code{"cov"} (covariance).
Default is \code{"cor"}.}

\item{method}{Character string specifying the correlation method: one of \code{"pearson"} (default), \code{"spearman"}, or \code{"kendall"}.
Ignored if \code{type = "cov"}.}
}
\value{
\itemize{
\item If no grouping is specified: returns a correlation or covariance matrix.
\item If a grouping formula is specified: returns a named list of matrices, one per group level.
}
}
\description{
Computes a correlation or covariance matrix for selected numeric variables in a data frame,
optionally grouped by a factor using a formula or bare variable names.
}
\examples{
# Correlation matrix for all numeric variables
iris |> compute_correlations()

# Covariance matrix for specific variables
iris |> compute_correlations(Sepal.Length, Petal.Length, type = "cov")

# Grouped correlation matrices by Species
iris |> compute_correlations(~ Species)

# Grouped correlation matrices for specific variables
iris |> compute_correlations(Sepal.Length, Petal.Length ~ Species)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/creator.R
\name{create_groups}
\alias{create_groups}
\title{Create Groups}
\usage{
create_groups(k, n, labels = seq_len(k))
}
\arguments{
\item{k}{Integer. Number of groups.}

\item{n}{Integer. Either:
\itemize{
\item A single value: total number of observations (must be divisible by \code{k}), or
\item A vector of length \code{k}: group sizes (must sum to total \code{n}).
}}

\item{labels}{Character vector of group labels. Defaults to \code{1:k}.}
}
\value{
A factor of length \code{sum(n)} with \code{k} groups in sequence.
}
\description{
Generate a factor with \code{k} groups and a specified number of observations.
Groups can be balanced (equal size) if \code{n} is a single integer, or weighted
(unequal size) if \code{n} is a vector of group sizes.
}
\examples{
# Example 1: Balanced groups (n = 12, k = 3 → 4 per group)
create_groups(k = 3, n = 12, labels = c("GroupA", "GroupB", "GroupC"))

# Example 2: Weighted groups (sizes 2, 5, 5)
create_groups(k = 3, n = c(2, 5, 5), labels = c("GroupA", "GroupB", "GroupC"))

# Example 3: Default labels when none supplied
create_groups(k = 3, n = c(4, 6, 2))

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filterer.R
\name{extract_intervals}
\alias{extract_intervals}
\title{Extract Point Estimate and Confidence Interval Columns}
\usage{
extract_intervals(x)
}
\arguments{
\item{x}{A data frame, matrix, list with 'estimate' and 'interval' (or 'conf.int') elements, or a t.test (htest) result.}
}
\value{
A matrix with columns: Estimate, LL, UL.
}
\description{
Extract the point estimate and confidence interval columns from a data frame, matrix, list, or t.test result.
}
\examples{
cbind(Estimate = c(10, 0, 5), LL = c(8, -1, 4), UL = c(12, 1, 6)) -> df
c("A", "B", "C") -> rownames(df)
df |> extract_intervals()

# t.test result
set.seed(1); rnorm(20, 10, 2) -> x
t.test(x) |> extract_intervals()
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotter.R
\name{plot_comp}
\alias{plot_comp}
\title{Plot Comparison of Two Groups with Difference Estimate}
\usage{
plot_comp(
  results,
  title = NULL,
  ylab = "Outcome",
  xlab = "",
  ylim = NULL,
  slab = "Difference",
  rope = NULL,
  digits = 3,
  values = FALSE,
  connect = FALSE,
  pos = c(2, 2, 4),
  pch = c(15, 15, 17),
  col = "black",
  offset = 0,
  points = TRUE,
  intervals = TRUE,
  lines = TRUE,
  ...
)
}
\arguments{
\item{results}{A data frame or matrix with three rows: two group estimates and a third row for
the difference (with placeholder values in the first two columns).}

\item{title}{Character string for the plot title. If \code{NULL}, uses the \code{comment()} attribute of \code{results}.}

\item{ylab}{Character label for the Y-axis.}

\item{xlab}{Character label for the X-axis.}

\item{ylim}{Numeric vector specifying Y-axis limits. If \code{NULL}, they are auto-computed.}

\item{slab}{Label for the secondary Y-axis (typically "Difference").}

\item{rope}{Numeric vector of length 2 defining a shaded region of practical equivalence (on the difference scale).}

\item{digits}{Number of decimal places for value labels if \code{values = TRUE}.}

\item{values}{Logical; if \code{TRUE}, displays numeric values beside points and intervals.}

\item{connect}{Logical; if \code{TRUE}, draws a connecting line between group points.}

\item{pos}{Integer vector specifying text positions (used for estimate, lower, upper labels).}

\item{pch}{Vector of plotting characters for each point (e.g., group1, group2, diff).}

\item{col}{Color for all plotted elements.}

\item{offset}{Numeric horizontal offset to avoid overlapping points.}

\item{points}{Logical; if \code{TRUE}, draws the three points.}

\item{intervals}{Logical; if \code{TRUE}, draws confidence intervals.}

\item{lines}{Logical; if \code{TRUE}, adds horizontal dashed lines from group points to difference estimate.}

\item{...}{Additional graphical parameters passed to \code{plot()}.}
}
\value{
Invisibly returns the \code{results} object (modified to include only point and interval columns).
}
\description{
Plots two group means and their confidence intervals, along with a third point showing the
estimated difference and its interval. Designed for paired or between-group comparisons.
}
\examples{
# Create comparison data for two groups and their difference
comp_data <- data.frame(
  estimate = c(0.6, 0.4, 0.2),
  ci_lower = c(0.4, 0.2, 0.0),
  ci_upper = c(0.8, 0.6, 0.4)
)
rownames(comp_data) <- c("Group 1", "Group 2", "Difference")

# Basic comparison plot
plot_comp(comp_data)

# Add ROPE, value labels, and connect groups
plot_comp(comp_data, rope = c(-0.1, 0.1), values = TRUE, connect = TRUE)

# Customize plotting characters and color
plot_comp(comp_data, pch = c(15, 17, 18), col = "darkgreen", lines = FALSE)

}

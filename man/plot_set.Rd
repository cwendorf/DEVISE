% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotter.R
\name{plot_set}
\alias{plot_set}
\title{Plot Point Estimates with Confidence Intervals}
\usage{
plot_set(
  results,
  title = NULL,
  ylab = "Outcome",
  xlab = "",
  ylim = NULL,
  digits = 3,
  offset = 0,
  pch = 16,
  col = "black",
  points = TRUE,
  intervals = TRUE,
  values = FALSE,
  pos = 2,
  connect = FALSE,
  line = NULL,
  rope = NULL,
  ...
)
}
\arguments{
\item{results}{A data frame or matrix containing point estimates in the first column, and
confidence limits in columns that match common naming patterns (e.g., "ll", "ul", "ci_lower", "ci_upper").}

\item{title}{Character string for the plot title. If \code{NULL}, the function uses the \code{comment()} attribute of \code{results}.}

\item{ylab}{Character label for the Y-axis.}

\item{xlab}{Character label for the X-axis.}

\item{ylim}{Numeric vector specifying Y-axis limits. If \code{NULL}, limits are auto-calculated.}

\item{digits}{Number of decimal places for value labels if \code{values = TRUE}.}

\item{offset}{Numeric horizontal offset to nudge plotted points (useful for overlapping points).}

\item{pch}{Plotting character (point shape).}

\item{col}{Color for points and interval lines.}

\item{points}{Logical; if \code{TRUE}, draws point estimates.}

\item{intervals}{Logical; if \code{TRUE}, draws confidence intervals.}

\item{values}{Logical; if \code{TRUE}, displays numeric values beside points and intervals.}

\item{pos}{Integer or vector specifying text position relative to points (see \code{text()}).}

\item{connect}{Logical; if \code{TRUE}, connects adjacent points with arrows.}

\item{line}{Numeric; if specified, draws a horizontal reference line at the given Y value.}

\item{rope}{Numeric vector of length 2 indicating a "region of practical equivalence" to shade.}

\item{...}{Additional graphical parameters passed to \code{plot()}.}
}
\value{
Invisibly returns the \code{results} object (modified to include only point and interval columns).
}
\description{
Creates an interval plot displaying point estimates and their associated lower and upper
confidence bounds for a set of outcomes. Useful for visualizing statistical estimates across
multiple conditions or variables.
}
\examples{
# Create example data frame with estimate and confidence intervals
results <- data.frame(
  estimate = c(0.3, 0.5, 0.7),
  ci_lower = c(0.1, 0.3, 0.5),
  ci_upper = c(0.5, 0.7, 0.9)
)
rownames(results) <- c("A", "B", "C")

# Basic interval plot
plot_set(results)

# Add value labels and a horizontal reference line at 0
plot_set(results, values = TRUE, line = 0)

# Customize point style and add rope
plot_set(results, pch = 19, col = "blue", rope = c(-0.1, 0.1))

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolver.R
\name{resolve}
\alias{resolve}
\title{Evaluate a Formula or Variable in a Data Frame}
\usage{
resolve(data, formula)
}
\arguments{
\item{data}{A data frame in which the formula or variable should be evaluated.}

\item{formula}{A formula (e.g., \code{y ~ x}) or a variable name.}
}
\value{
The result of evaluating the formula or variable in the data frame context.
\itemize{
\item If a single variable is provided, returns the vector.
\item If a formula is provided, returns the formula object suitable for functions like \code{lm()}.
}
}
\description{
\code{resolve()} evaluates a formula or a single variable within the context of a given data frame.
This is useful for pipe-friendly workflows where you want to dynamically compute expressions
using columns of a data frame.
}
\examples{
df <- data.frame(
  group = rep(c("A", "B"), each = 5),
  score = c(5, 6, 7, 5, 6, 8, 9, 7, 8, 9)
)

df |> resolve(score ~ group) |> t.test()
df |> resolve(score ~ group) |> lm() |> summary()
df |> resolve(score ~ group) |> aov()
df |> resolve(score)

}

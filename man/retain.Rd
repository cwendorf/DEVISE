% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluator.R
\name{retain}
\alias{retain}
\title{Retain an Object in a Pipeline}
\usage{
retain(x, name)
}
\arguments{
\item{x}{The object to retain (typically coming from a pipeline).}

\item{name}{A symbol giving the name to assign \code{x} to in the
caller's environment.}
}
\value{
Invisibly returns \code{x}, unchanged.
}
\description{
\code{retain()} assigns an object in the caller's environment
while returning the value invisibly. This makes it easy to save
intermediate results when using the native pipe (\verb{|>}).
}
\examples{
# Save a filtered dataset in a pipeline
iris |>
  subset(Species == "setosa") |>
  retain(setosa_only) |>
  summary()

# Save a model fit while still viewing its summary
iris |>
  lm(Sepal.Length ~ Sepal.Width, data = _) |>
  retain(model_fit) |>
  summary()

# Save an intermediate step for debugging
mtcars |>
  subset(cyl == 6) |>
  retain(six_cyls) |>
  transform(kpl = mpg * 0.425) |>
  head()
}

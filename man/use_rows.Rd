% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filterer.R
\name{use_rows}
\alias{use_rows}
\title{Filter Rows Referenced by a Formula}
\usage{
use_rows(formula, condition)
}
\arguments{
\item{formula}{A two-sided formula (for example, \code{Outcome ~ Group}).}

\item{condition}{A logical condition evaluated in the formula's model frame.}
}
\value{
A formula with the same left- and right-hand sides as \code{formula}, but
whose environment contains only filtered rows.
}
\description{
\code{use_rows()} filters the variables referenced in a two-sided formula and
returns the same formula with an updated environment containing only the
filtered rows. This allows formula-based functions (for example, \code{t.test()})
to be used in a pipe-friendly workflow without creating temporary objects.
}
\details{
If \code{condition} is written as \code{x == c("a", "b")}, it is treated as
membership (\code{x \%in\% c("a", "b")}) to avoid element-wise recycling.
}
\examples{
# Base R t test with formula filtering in a pipe
gl(3, 10, labels = c("Level1", "Level2", "Level3")) -> Factor
c(6, 8, 6, 8, 10, 8, 10, 9, 8, 7,
  7, 13, 11, 10, 13, 8, 11, 14, 12, 11,
  9, 16, 11, 12, 15, 13, 9, 14, 11, 10) -> Outcome

(Outcome ~ Factor) |>
  use_rows(Factor == c("Level1", "Level2")) |>
  t.test() |>
  extract_intervals()

# Equivalent explicit condition form
(Outcome ~ Factor) |>
  use_rows(Factor \%in\% c("Level2", "Level3")) |>
  t.test()
}

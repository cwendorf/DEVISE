% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluator.R
\name{use_vars}
\alias{use_vars}
\title{Select Variables or Formulas from a Data Frame}
\usage{
use_vars(data, ...)
}
\arguments{
\item{data}{A data frame providing the evaluation context.}

\item{...}{One or more expressions:
\itemize{
\item A formula (e.g., \code{y ~ x1 + x2}) for use with modeling functions.
\item One or more bare variable names (e.g., \verb{x1, x2}).
}}
}
\value{
\itemize{
\item If a single variable is provided: returns a vector.
\item If multiple variables are provided: returns a data frame.
\item If a formula is provided: returns a data frame with the selected variables,
and the original formula is preserved as an attribute for use by functions
like \code{lm()}, \code{glm()}, or \code{t.test()}.
}
}
\description{
\code{use_vars()} evaluates one or more expressions within the context of a data frame.
This can include formulas (e.g., \code{y ~ x}) or bare variable names. The result
is pipe-friendly and adapts depending on whether you select a single variable,
multiple variables, or a formula.
}
\examples{
data.frame(
  y = 1:5,
  x1 = c(1,1,1,2,2),
  x2 = c(5,4,3,2,1),
  x3 = c(10,20,30,40,50)
) -> df

# Use a formula for modeling
df |> use_vars(y ~ x1) |> t.test()
df |> use_vars(y ~ x1 + x2) |> lm() |> summary()

# Use a single variable
df |> use_vars(x3)

# Use multiple variables
df |> use_vars(x1, x2) |> cor()

}
